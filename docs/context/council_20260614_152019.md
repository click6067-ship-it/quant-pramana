**VERDICT: REVISE**  
판정 라벨: `RESEARCH_ONLY / PRODUCTION_UNSAFE`

핵심: 6개 중 4개는 고쳐졌고, 2개는 부분 수정입니다. STOP은 아님. Attack/Moonshot 결과가 look-ahead로 좋아졌다는 증거는 못 봤고, SEC API도 submissions JSON이 공시 disseminate 시점에 실시간 갱신된다고 설명합니다. 다만 현재 산출물/문서 정직성은 아직 맞지 않습니다. SEC API 근거: submissions JSON은 “real time” 갱신, bulk는 매일 약 3:00 a.m. ET 재발행이라고 명시되어 있습니다. ([sec.gov](https://www.sec.gov/search-filings/edgar-application-programming-interfaces)) 백테스트 과최적화 경계도 여전히 맞습니다. Carr/Lopez de Prado는 historical simulation으로 trading rule을 calibrate하는 행위가 backtest overfitting에 기여한다고 명시합니다. ([arxiv.org](https://arxiv.org/abs/1408.1159?utm_source=openai))

**6개 재검수**

1. **FIXED** — Attack kill #④ 오기  
   [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:61)에서 smallmid는 ②③만 fail, sp500은 ①②③으로 정정됨. JSON도 일치: smallmid fails는 ②③, sp500 fails는 ①②③. [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:225)의 `verdict_attack`에 dead no-op 라인 없음.

2. **PARTIAL** — decision_log append-only 정정  
   코드 본문 주석과 리포트는 “append-only 아님, 결정적 재생성”으로 정정됨: [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:127), [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:35). `run_id/as_of` 컬럼도 들어감: [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:130).  
   하지만 파일 상단 docstring은 아직 “decision_log append-only”라고 남아 있음: [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:8). 더 나쁜 건 현재 `decision_log.csv`는 4,290줄뿐인데 리포트는 60,136행이라고 주장함: [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:28). 원인: `keep.to_csv(DLOG, index=False)`가 마지막 universe 실행으로 덮어씀: [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:144).

3. **FIXED** — EDGAR `_avail_date` ET wall-clock  
   [a2_event_store.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_event_store.py:69)에서 tz 제거 후 wall-clock 처리. 기존 UTC-style threshold와 현재 산출 `available_at` 비교도 34,381 EDGAR rows에서 mismatch 0으로 나와 결과 robust. 단, “Z suffix가 실제 ET wall-clock”이라는 주장은 코드 주석보다 별도 근거 파일에 박는 게 낫다.

4. **FIXED** — survivorship censoring 계량  
   코드 summary에 `n_trigger_before_return_filter`, `n_dropped_no_exit_censoring`, `censoring_note` 포함: [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:147). 리포트도 smallmid 65,178→2,290 제외=3.5%, sp500 4,384→11 제외를 명시: [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:42).

5. **PARTIAL** — Moonshot coverage test 강등  
   §3에서는 “전종목 아님, EDGAR 표본 200종 coverage test, NEEDS_EVIDENCE/INSUFFICIENT”로 제대로 낮춤: [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:68).  
   하지만 제목/결론에는 아직 “Attack/Moonshot 완전 구현”, “둘 다 robust edge 없음” 표현이 남아 과함: [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:95). Moonshot은 “표본 내 edge 없음 / capital veto 충분 / research evidence insufficient”가 정확함.

6. **FIXED** — smallmid 평균 +2% 부활 금지  
   명시됨. [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:99)에 DSR/PBO류 보정 없이 양평균 살리기 금지, “우측꼬리 있으니 Attack 살리자” 금지까지 박혀 있음.

**새 발견 / Objections**

- [발견] `decision_log.csv`가 두 universe를 보존하지 않고 마지막 실행으로 덮어쓴다.  
  왜 실패: 리포트의 “60,136행 decision log”와 현재 파일 4,290줄이 충돌.  
  임팩트: audit artifact가 재현 설명과 불일치.  
  수정안: `decision_log_{universe}.csv`로 분리하거나 combined 파일을 명시적으로 생성.

- [발견] decision log에 `fwd_5`, `qqq_fwd_5` 미래 실현값이 들어간다.  
  왜 실패: 백테스트 계산에는 영향 없어도 “decision log”라는 이름과 충돌한다.  
  임팩트: 누군가 downstream에서 decision-time artifact로 쓰면 미래정보 컬럼이 섞인다.  
  수정안: `decision_log`에는 planned-only, realized returns는 `outcome_log`로 분리.

- [발견] Moonshot 표본은 `feature_store` 전기간 빈출 종목 기반이다.  
  왜 실패: coverage test로는 허용되지만 PIT universe 검증은 아니다.  
  임팩트: Moonshot negative 결론은 괜찮지만 “완전 검증” 문구는 금지.  
  수정안: 결론 문구를 “EDGAR sample coverage test negative”로만 유지.

**가장 먼저 고칠 1개**

`decision_log` 산출물 불일치. 파일 분리 또는 combined 생성 후 리포트의 60,136행 주장과 실제 파일을 맞춰라. 지금 상태로는 audit receipt가 거짓이다.
