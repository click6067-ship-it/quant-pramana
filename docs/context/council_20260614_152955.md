판정 라벨: `RESEARCH_ONLY / PRODUCTION_UNSAFE`  
VERDICT: `SHIP` with one hygiene fix. 계산오류/look-ahead로 STOP할 건 못 봤다.

1. [발견] `decision_log_{universe}.csv` 수정은 핵심 FIXED  
왜 실패 아님: universe별 파일은 planned-only 컬럼만 있고 미래값 없다. 행수도 헤더 제외 `smallmid=60,136`, `sp500=4,289`로 리포트와 일치. realized는 `outcome_log_{universe}.csv`로 분리됨. PIT/temporal leakage는 금융 백테스트에서 치명적이라는 외부 prior와도 맞다: Look-Ahead-Bench도 PIT 금융 워크플로우에서 look-ahead bias를 별도 평가 대상으로 둔다. ([arxiv.org](https://arxiv.org/abs/2601.13770?utm_source=openai))  
임팩트: SHIP 가능.  
수정안: 단, legacy `phase1a/outputs/a2_decisions/decision_log.csv`는 여전히 `fwd_5`, `qqq_fwd_5`를 포함한다. 가장 먼저 고칠 1개 = `a2_blind_backtest.py:146`의 legacy write를 없애거나 `legacy_outcome_mixed_log.csv`로 이름 바꿔라.

2. [발견] docstring의 “append-only” 잔재는 FIXED  
왜 실패 아님: [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:8)는 “동결캐시서 결정적 재생성”으로 정리됐다. 리포트도 [A2_attack_moonshot_blind_backtest.md](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:35)에서 append-only 아님을 명시한다.  
임팩트: 정직성 라벨 OK.  
수정안: 없음.

3. [발견] Moonshot 과장 라벨은 FIXED  
왜 실패 아님: 제목/결론이 “완전 검증”이 아니라 `EDGAR 표본 200종 coverage test`, `edge 없음/NEEDS_EVIDENCE`로 낮아졌다. OpenEDGAR 논문도 EDGAR 기반 연구 DB는 filings/index/metadata를 구성하는 인프라 문제라고 설명한다. 표본 coverage를 전종목 검증으로 포장하지 않는 현재 표현이 맞다. ([arxiv.org](https://arxiv.org/abs/1806.04973?utm_source=openai))  
임팩트: capital veto에는 충분, research proof에는 부족이라는 분리가 맞다.  
수정안: 없음.

4. [발견] DCA 숫자 버그는 FIXED, 용어는 PARTIAL  
왜 실패 아님: [a2_benchmarks.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_benchmarks.py:60)는 `market_value / invested`라서 per-unit 폭증 버그가 사라졌다. 산출도 `+794.7%`, `MDD -83.6%`로 리포트와 일치. DCA는 고정 금액을 정기 투자하는 방식이라는 외부 정의와도 맞다. ([investopedia.com](https://www.investopedia.com/articles/forex/052815/pros-cons-dollar-cost-averaging.asp?utm_source=openai))  
임팩트: 계산은 OK. 다만 이것은 엄밀한 IRR/MWRR가 아니라 “총투입 대비 최종가치 배수”다.  
수정안: `money-weighted` 대신 `contribution-weighted capital multiple (value / total contributed)`라고 쓰면 더 정확하다.

5. [발견] smallmid 평균 +2% 부활 금지는 FIXED  
왜 실패 아님: 리포트 [라인 99](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/reports/A2_attack_moonshot_blind_backtest.md:99)가 DSR/PBO 없이 양의 평균 살리기를 금지한다. DSR는 selection bias, backtest overfitting, sample length, non-normality를 보정하려는 도구라는 외부 근거와 정합적이다. ([en.wikipedia.org](https://en.wikipedia.org/wiki/Deflated_Sharpe_ratio?utm_source=openai))  
임팩트: 우측꼬리 lottery를 edge로 둔갑시키는 길이 닫혔다.  
수정안: 없음.

6. [발견] 남은 리스크는 계산이 아니라 명명 혼동  
왜 실패: [a2_blind_backtest.py](/home/click/ghq/github.com/click6067-ship-it/quant-pramana/phase1a/engine/a2_blind_backtest.py:273)가 아직 `decision_log → {DLOG}`를 출력한다. 그런데 그 `DLOG`는 planned-only가 아니다.  
임팩트: 나중 council이 파일명만 보고 “decision_log에 미래값 있다”를 다시 물 가능성이 높다.  
수정안: legacy combined 파일 write/print 제거. 이것만 먼저 고쳐라.

최종: `SHIP`. 남은 건 정직성 라벨/파일명 hygiene이고, 요청 범위의 look-ahead·행수·Moonshot 과장·DCA per-unit 계산오류는 해결된 것으로 본다.
